package com.example.rest.config;

public interface Constants {

    String USER_ALREADY_EXIST_CODE = "USER_ALREADY_EXIST";
    String USER_ALREADY_EXIST_DESCRIPTION = "A user with the given username already exists!";

    String USERNAME_OR_PASSWORD_REGEXP = "^[A-Za-z0-9]*$";
}
