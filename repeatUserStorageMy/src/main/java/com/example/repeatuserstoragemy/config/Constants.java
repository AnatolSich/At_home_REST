package com.example.repeatuserstoragemy.config;

public interface Constants {
    String ERROR_CODE = "USER_ALREADY_EXIST";
    String ERROR_DESCRIPTION = "User with given name is already exist";
    String USERNAME_PASSWORD_REGEXP = "^[A-Za-z0-9]*$";
}
